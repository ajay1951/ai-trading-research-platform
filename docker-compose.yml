# Multi-Agent Financial Intelligence System - Docker Compose
# Orchestrates all services: main app, dashboard, and optional storage backends

services:
  # Main application
  app:
    build: .
    container_name: financial-ai-app
    restart: unless-stopped
    ports:
      - "8000:8000"  # Dashboard API
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - USE_REDIS=${USE_REDIS:-false}
      - USE_CHROMADB=${USE_CHROMADB:-false}
      - USE_INFLUXDB=${USE_INFLUXDB:-false}
      - DOCKER_ENV=true
      - REDIS_HOST=redis
      - CHROMADB_HOST=chromadb
      - INFLUXDB_HOST=influxdb
      - POSTGRES_HOST=postgres
      - POSTGRES_USER=financial_admin
      - POSTGRES_PASSWORD=financial_secure_password
      - POSTGRES_DB=ai_crypto_db
    depends_on:
      - redis
      - chromadb
      - influxdb
      - postgres
    volumes:
      - .:/app
    dns:
      - 8.8.8.8
      - 8.8.4.4
    networks:
      - financial-ai-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Live Trading Bot
  bot:
    build: .
    container_name: financial-ai-bot
    restart: unless-stopped
    command: python scripts/live_trader.py
    environment:
      - BINANCE_API_KEY=${BINANCE_API_KEY}
      - BINANCE_SECRET_KEY=${BINANCE_SECRET_KEY}
      - USE_INFLUXDB=true
      - REDIS_HOST=redis
    depends_on:
      - influxdb
    volumes:
      - .:/app
    networks:
      - financial-ai-network

  # MLOps Continuous Learning Pipeline
  mlops:
    build: .
    container_name: financial-ai-mlops
    restart: unless-stopped
    # Runs the retraining script once, then sleeps for 7 days (604800 seconds)
    command: sh -c "while true; do python scripts/mlops_retrainer.py; sleep 604800; done"
    depends_on:
      - influxdb
    volumes:
      - .:/app
    networks:
      - financial-ai-network

  # Redis for pub/sub and caching
  redis:
    image: redis:7-alpine
    container_name: financial-ai-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes
    networks:
      - financial-ai-network
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 256M
        reservations:
          cpus: '0.10'
          memory: 64M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s

  # ChromaDB for vector search
  chromadb:
    image: chromadb/chroma:latest
    container_name: financial-ai-chromadb
    restart: unless-stopped
    ports:
      - "8001:8000"
    volumes:
      - chroma-data:/chroma/chroma_data
    environment:
      - IS_PERSISTENT=TRUE
      - ANONYMIZED_TELEMETRY=FALSE
    networks:
      - financial-ai-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 30s

  # InfluxDB for time-series data
  influxdb:
    image: influxdb:2.7
    container_name: financial-ai-influxdb
    restart: unless-stopped
    ports:
      - "8086:8086"
    volumes:
      - influx-data:/var/lib/influxdb2
      - influx-config:/etc/influxdb2
    environment:
      - DOCKER_INFLUXDB_INIT_MODE=setup
      - DOCKER_INFLUXDB_INIT_USERNAME=admin
      - DOCKER_INFLUXDB_INIT_PASSWORD=admin123
      - DOCKER_INFLUXDB_INIT_ORG=financial_ai
      - DOCKER_INFLUXDB_INIT_BUCKET=market_data
    networks:
      - financial-ai-network
    deploy:
      resources:
        limits:
          cpus: '0.75'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M
    healthcheck:
      test: ["CMD", "influx", "bucket", "list"]
      interval: 30s

  # PostgreSQL for RL State Persistence
  postgres:
    image: postgres:15-alpine
    container_name: financial-ai-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=financial_admin
      - POSTGRES_PASSWORD=financial_secure_password
      - POSTGRES_DB=ai_crypto_db
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - financial-ai-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U financial_admin -d ai_crypto_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  redis-data:
  chroma-data:
  influx-data:
  influx-config:
  postgres-data:

networks:
  financial-ai-network:
    driver: bridge
